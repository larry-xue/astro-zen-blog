---
import ThemeToggle from './ThemeToggle.astro';
import { siteConfig } from '../config';
import Search from './Search.astro';

const navItems = [
  { href: '/', text: 'Home' },
  { href: '/blog', text: 'Blog' },
  { href: '/tags', text: 'Tags' },
  { href: '/archive', text: 'Archive' },
  { href: 'https://github.com/larry-xue/astro-zen-blog', text: 'Repo' },
];
---

<header class="py-6">
  <nav class="flex justify-between items-center flex-wrap">
    <a href="/" class="text-2xl font-bold text-gray-900 dark:text-white">{ siteConfig.title }</a>
    <div class="flex min-w-0 items-center gap-3 sm:gap-6">
      <ul class="flex min-w-0 flex-wrap gap-x-4 gap-y-1 sm:gap-6">
        {navItems.map(({ href, text }) => (
          <li>
            <a
              href={href}
              class="text-gray-700 dark:text-gray-100 hover:text-gray-900 dark:hover:text-white"
            >
              {text}
            </a>
          </li>
        ))}
      </ul>
      {siteConfig.search && <Search />}
      <ThemeToggle />
    </div>
  </nav>
</header>
